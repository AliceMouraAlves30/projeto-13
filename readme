# Projeto de Análise de Dados

Este projeto tem como objetivo realizar a análise exploratória de dados de diversos arquivos CSV, incluindo a geração de gráficos e a análise estatística descritiva. A análise é realizada a partir de arquivos armazenados no Google Drive.

## Funcionalidades

- Carregamento de dados a partir de URLs.
- Análise exploratória dos dados:
  - Visualização das primeiras linhas do DataFrame.
  - Informações sobre o DataFrame.
  - Estatísticas descritivas das variáveis numéricas.
  - Geração de histogramas, gráficos de barras, gráficos de pizza, gráficos de linha, gráficos de dispersão, boxplots e matrizes de correlação.
- Análise especializada dos dados.

## Requisitos

- Python 3.x
- Pandas
- Matplotlib
- Seaborn
- Requests

## Instalação

1. Clone o repositório:
    ```bash
    git clone https://github.com/seu-usuario/seu-repositorio.git
    cd seu-repositorio
    ```

2. Crie um ambiente virtual e ative-o:
    ```bash
    python -m venv venv
    source venv/bin/activate  # Linux/macOS
    venv\Scripts\activate     # Windows
    ```

3. Instale as dependências:
    ```bash
    pip install -r requirements.txt
    ```

## Uso

1. Edite a lista `urls` no arquivo `main.py` para incluir as URLs dos arquivos CSV que deseja analisar.

2. Execute o script:
    ```bash
    python main.py
    ```

## Estrutura do Projeto

